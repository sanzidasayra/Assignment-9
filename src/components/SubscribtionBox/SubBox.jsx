import React from 'react';
import SubscriptionBox from './SubscriptionBox';

const SubBox = ({ singleSubscriptionBox }) => {
  return (
    <SubscriptionBox singleSubscriptionBox={singleSubscriptionBox} />
  );
};

export default SubBox;
